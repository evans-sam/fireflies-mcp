services:
  fireflies-mcp:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - FIREFLIES_API_KEY=${FIREFLIES_API_KEY}
      - TRANSPORT=http
      - PORT=3000
    restart: unless-stopped
